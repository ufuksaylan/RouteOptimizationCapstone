// Only sharing the types, not the entities themselves!
// Types are stripped out by the compiler, so this is safe.
export type * from '../entities/location';
export type * from '../entities/trip';
export type * from '../entities/user';

